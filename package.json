{
  "name": "yarn-workspaces-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:react-app": "yarn workspace react-app start",
    "start:next-app": "yarn workspace next-app dev"
  },
  "workspaces": [
    "apps/*",
    "packages/**/*"
  ]
}
